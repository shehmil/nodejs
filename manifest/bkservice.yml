apiVersion: v1
kind: Service
metadata:
  name: nodejsBK-service
spec:
  selector:
    app: nodejs 
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 3000
    nodePort: 31110
