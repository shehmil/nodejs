
apiVersion: v1
kind: Service
metadata:
  name: nodejss
spec:
  selector:
    app: nodejss
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer



#apiVersion: v1
#kind: Service
#metadata:
 # name: nodejs
#spec:
 # type: NodePort
 # ports:
 # - name: http
 #   port: 5000
 #   targetPort: 3000
 #   protocol: TCP
#  selector:
#    app: nodejs
